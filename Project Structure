black-mamba-bot/
│
├── backend/
│   ├── app.py                  # Flask + Socket.IO server with Quotex WebSocket client 
│   ├── requirements.txt       # Python dependency list
│
└── frontend/
    ├── index.html             # Signal Panel UI (HTML + JS + CSS)
    └── styles.css             # CSS স্টাইল (optional separate file)
